import { useState } from "react";


function Inputs() {
  const [points, setPoints] = useState('');
  const [multiplier, setMultiplier] = useState('');
  

  return ( 
    <>
      <p>the points, multiplier and start button comes here</p>
    </> 
  );
}

export default Inputs;